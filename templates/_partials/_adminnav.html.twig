<aside class="col-3 border border-light bg-white p-2">

    <article class="card mb-3">
        <div class="card-header">
            Catégories
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_categorie_index') }}">Liste des catégories</a></p>
            <p><a href="">Ajouter une catégorie</a></p>
        </div>
    </article>

    <article class="card mb-3">
        <div class="card-header">
            Plats
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_plats_index') }}">Liste des plats</a></p>
            <p><a href="{{ path('admin_plats_add') }}">Ajouter un plats</a></p>
        </div>
    </article>

    <article class="card mb-3">
        <div class="card-header">
            Utilisateurs
        </div>
        <div class="card-body">
            <p><a href="{{ path('admin_users_index') }}">Liste des utilisateurs</a></p>
        </div>
    </article>
</aside>