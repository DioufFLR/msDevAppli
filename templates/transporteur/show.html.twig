{% extends 'base.html.twig' %}

{% block title %}Transporteur{% endblock %}

{% block body %}

    <main class="container">
        <h1>Transporteur</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transporteur.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ transporteur.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ transporteur.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ transporteur.prix }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('app_transporteur_index') }}">back to list</a>

        <a href="{{ path('app_transporteur_edit', {'id': transporteur.id}) }}">edit</a>

        {{ include('transporteur/_delete_form.html.twig') }}
    </main>

{% endblock %}
