<form method="post" action="{{ path('app_transporteur_delete', {'id': transporteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporteur.id) }}">
    <button class="btn">Delete</button>
</form>
